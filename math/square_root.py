# Newton's method for caculating square root
# for computing x = a^(0.5), simply approximate with X(n+1) = 1/2(Xn + a/Xn) with some X1 > 0

#Therefore complexity for computing sqrt(n) is generally O(logn) when dealing with bounded input word length